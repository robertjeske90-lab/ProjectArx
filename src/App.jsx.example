/**
 * APP.JSX - Integration der neuen Module
 * 
 * Füge diese Änderungen in deine bestehende App.jsx ein:
 */

// === IMPORTS HINZUFÜGEN ===
import { DiceProvider } from './contexts/DiceContext';

// === APP COMPONENT WRAPPEN ===
// Deine bestehende App-Struktur muss mit dem DiceProvider umschlossen werden:

/*
function App() {
  return (
    <DiceProvider>
      <BrowserRouter>
        <div className="min-h-screen bg-arx-darker text-white">
          <Navbar />
          <Routes>
            <Route path="/" element={<Home />} />
            <Route path="/characters" element={<Characters />} />
            <Route path="/character/:id" element={<CharacterDetail />} />
            <Route path="/create" element={<CharacterCreatorSlideshow />} />
            {/* ... weitere Routes *//*}
          </Routes>
        </div>
      </BrowserRouter>
    </DiceProvider>
  );
}
*/

// ============================================================
// VOLLSTÄNDIGES BEISPIEL
// ============================================================

import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import { DiceProvider } from './contexts/DiceContext';

// Pages
import Home from './pages/Home';
import Characters from './pages/Characters';
import CharacterDetail from './pages/CharacterDetail';
import CharacterCreatorSlideshow from './pages/CharacterCreatorSlideshow';

// Components
import Navbar from './components/Navbar';

function App() {
  return (
    <DiceProvider>
      <BrowserRouter>
        <div className="min-h-screen bg-arx-darker text-white">
          <Navbar />
          <main>
            <Routes>
              <Route path="/" element={<Home />} />
              <Route path="/characters" element={<Characters />} />
              <Route path="/character/:id" element={<CharacterDetail />} />
              <Route path="/create" element={<CharacterCreatorSlideshow />} />
            </Routes>
          </main>
        </div>
      </BrowserRouter>
    </DiceProvider>
  );
}

export default App;
